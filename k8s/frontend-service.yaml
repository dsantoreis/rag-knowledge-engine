apiVersion: v1
kind: Service
metadata:
  name: cortex-frontend
  labels:
    app: cortex-frontend
spec:
  selector:
    app: cortex-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
